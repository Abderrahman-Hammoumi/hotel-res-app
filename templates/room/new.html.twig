{% extends 'base.html.twig' %}

{% block title %}Add New Room{% endblock %}

{% block body %}
<div class="row justify-content-center mt-4">
    <div class="col-md-6">
        <div class="card shadow-sm border-0">
            <div class="card-header bg-primary text-white">
                <h4 class="mb-0">Add New Room</h4>
            </div>

            <div class="card-body p-4">
                {{ include('room/_form.html.twig', {'button_label': 'Save Room'}) }}

                <div class="mt-3">
                    <a class="btn btn-outline-secondary" href="{{ path('app_room_index') }}">Back to list</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
